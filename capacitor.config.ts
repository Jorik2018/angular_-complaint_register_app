import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'gob.regionancash.pe.denunciaform',
  appName: 'Formulario de Denuncias GRA',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
