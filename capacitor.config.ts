import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'angular_complaint_app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
